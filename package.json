{
  "name": "jallua-ui",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf factories && rm -rf scripts && rm -rf dist/scripts",
    "ngc": "ngc",
    "build": "tsc",
    "rollup": "rollup -c -i bootstrap.js -o dist/scripts/bundle.js -c ",
    "rollup-dev": "rollup -c -i bootstrap-dev.js -o dist/scripts/bundle.js -c ",
    "postinstall": "npm run ngc && npm run build && npm run rollup",
    "start-dev": "npm run clean && npm run build && npm run rollup-dev && concurrently \"tsc -w\" \"rollup -w -c -i bootstrap-dev.js -o dist/scripts/bundle.js\" \"lite-server\""
  },
  "licences": [
    {
      "type": "MIT"
    }
  ],
  "dependencies": {
    "@angular/common": "~2.2.1",
    "@angular/compiler": "~2.2.1",
    "@angular/compiler-cli": "~2.2.1",
    "@angular/core": "~2.2.1",
    "@angular/http": "~2.2.1",
    "@angular/platform-browser": "~2.2.1",
    "@angular/router": "~3.2.1",
    "@types/node": "^6.0.46",
    "angular-in-memory-web-api": "~0.1.15",
    "angular2-google-maps": "^0.16.0",
    "reflect-metadata": "^0.1.8",
    "rollup": "^0.36.3",
    "rollup-plugin-commonjs": "^5.0.5",
    "rollup-plugin-node-resolve": "^2.0.0",
    "rxjs": "~5.0.0-rc.4",
    "typescript": "^2.0.9",
    "zone.js": "^0.6.25"
  },
  "devDependencies": {
    "@angular/platform-browser-dynamic": "^2.2.1",
    "concurrently": "^3.0.0",
    "lite-server": "^2.2.2",
    "rollup-watch": "^2.5.0"
  }
}
